# 🎓 מדריך הצגת הקוד — Nights Of Zahara

מדריך להצגת הפרויקט למרצה: מה לפתוח, באיזה סדר, מה להדגיש, ואיך לענות על שאלות.
המשחק: משחק סימולציית-חיים / RPG קליל לאייפון בהשראת *אלף לילה ולילה*, בנוי ב-**SwiftUI + MVVM**, עם שמירה מקומית ב-`Codable`.

---

## 1. פתיחה — משפט הפתיחה (30 שניות)

> "בניתי משחק תפקידים לאייפון בשם *Nights Of Zahara*. השחקן חי לילה-אחר-לילה בעיר קסומה, מוציא אנרגיה על פעולות שמשפרות תכונות, כובש מבוכים כדי לקשור *ג'ינים*, ובונה לגנדה. המשחק כתוב ב-SwiftUI בארכיטקטורת MVVM, עם שמירה מקומית מלאה ותאימות-לאחור לשמירות ישנות."

**מה חשוב שהמרצה יבין מיד:**
- זה פרויקט **מלא ועובד** (רץ בסימולטור, נשמר, ממשיך).
- הבחירות הארכיטקטוניות מכוונות: **הפרדת אחריות** בין Model / ViewModel / View.

---

## 2. ארכיטקטורה — הדבר הכי חשוב להראות

הפרויקט בנוי ב-**MVVM** (Model–View–ViewModel). זהו הלב של ההצגה.

```
┌─────────────┐     observes (@EnvironmentObject)    ┌──────────────────┐
│    Views    │ ───────────────────────────────────▶ │  GameViewModel   │
│  (SwiftUI)  │ ◀─────────────────────────────────── │ (ObservableObject)│
└─────────────┘        @Published state changes       └──────────────────┘
                                                              │
                                                     mutates & saves
                                                              ▼
                                                       ┌──────────────┐
                                                       │  GameState   │
                                                       │  (Codable)   │
                                                       └──────────────┘
                                                              │
                                                       SaveManager
                                                       (JSON + UserDefaults)
```

**שלוש השכבות בקוד:**

| שכבה | תיקייה | תפקיד |
|------|--------|-------|
| **Model** | `Models/` | נתונים טהורים, ברובם `Codable`. אין להם לוגיקת UI. |
| **ViewModel** | `Engine/GameViewModel*` | כל הלוגיקה: פעולות, קרבות, מבוכים, זרימת הלילה. מפרסם `@Published`. |
| **View** | `Views/` | תצוגה בלבד ב-SwiftUI. צופה ב-ViewModel, לא משנה נתונים ישירות. |

**המשפט להגיד:** "כל שינוי במצב המשחק עובר דרך ה-ViewModel, שמעדכן `GameState` ושומר אוטומטית. ה-Views רק *צופים* ומרנדרים — אין להם לוגיקה עסקית."

---

## 3. סדר הצגת הקבצים (ה-Script)

הצג לפי הסדר הזה — מהתשתית אל הפרטים:

### שלב א' — נקודת הכניסה
1. **`NightsOfZaharaApp.swift`** — נקודת הכניסה של האפליקציה.
2. **`ContentView.swift`** — ה-Router הראשי. הראה את ה-`switch game.phase` (onboarding / playing / ended) ואת ה-`@StateObject private var game = GameViewModel()` — כאן ה-ViewModel נוצר פעם אחת ומשותף לכל העץ דרך `.environmentObject(game)`.

### שלב ב' — המודל
3. **`Models/GameState.swift`** — ה-snapshot המלא שנשמר: לילה, תכונות, זהב, מלאי, ג'ינים. הצבע על `Codable`.
4. **`Models/Stats.swift`** — 10 התכונות, ה-`subscript` שמגביל לתקרה 125, וה-custom decoder (ברירת מחדל ל-Speed בשמירות ישנות).
5. **`Models/MetaState.swift`** — **הנקודה החזקה לתאימות-לאחור**: כל התוכן המורחב יושב באובייקט אופציונלי אחד, כך שגם שמירה ישנה ממשיכה להיטען. הסבר את `decodeIfPresent ?? fallback`.

### שלב ג' — ה-ViewModel (הליבה)
6. **`Engine/GameViewModel.swift`** — הראה את `perform(_:)` (ה-dispatch של הפעולות) ואת `endNight()` (זרימת סוף לילה + סיכום).
7. הצבע על החלוקה ל-**Extensions** לפי אחריות:
   - `GameViewModel+Features.swift` — מלאי, ציוד, פלגים, תארים.
   - `GameViewModel+Features2.swift` — משימות, ייצור, שדרוגי בית.
   - `GameViewModel+Characters.swift` — הדמויות הגדולות.
   > "פירקתי ViewModel גדול לקבצים לפי תחום אחריות, כדי לשמור על קריאוּת."

### שלב ד' — ה-View וההתמדה
8. **`Views/CityView.swift`** או **`NightActionsView.swift`** — הראה איך View צופה ב-`game.state` ומרנדר, בלי לוגיקה.
9. **`Engine/SaveManager.swift`** — שמירה כ-JSON ל-Documents + מראה ב-`UserDefaults`, וטעינה עם fallback.

---

## 4. הדגמה חיה (Demo) — להריץ בסימולטור

הרץ את האפליקציה והדגם זרימה קצרה:
1. **מסך פתיחה** → "Begin a New Legend" → יצירת דמות (בחירת תפקיד).
2. **מסך City** — הצבע על ה-HUD (לילה / אנרגיה / זהב) ועל *At a Glance*.
3. **מסך Actions** — בחר פעולה (למשל *Work*), הראה את כרטיס התצוגה-המקדימה עם עלות האנרגיה והסיכוי.
4. **סיים לילה** — הראה את מסך **סיכום הלילה** (Night Summary).
5. **הראה שמירה עובדת** — סגור והפעל מחדש; המשחק ממשיך מאותה נקודה.

> טיפ: אם אין זמן להגיע לתוכן מתקדם, אפשר לטעון שמירה מוכנה שמדגימה שלב מתקדם (מבוכים, ג'ינים).

**הפקודה להרצה מהטרמינל (אם צריך):**
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild \
  -project NightsOfZahara.xcodeproj -scheme NightsOfZahara \
  -destination 'platform=iOS Simulator,name=iPhone 17' build
```

---

## 5. נקודות טכניות חזקות להדגיש

בחר 3–4 מתוכן — אלו מרשימות מרצה:

- **תאימות-לאחור לשמירות** — `MetaState` אופציונלי + decoders מותאמים. המשחק גדל, שמירות ישנות עדיין נטענות. *(הכי חזק)*
- **State management נכון ב-SwiftUI** — `@StateObject` נוצר פעם אחת ב-root, מופץ כ-`@EnvironmentObject`. אין "מקורות אמת" כפולים.
- **`Codable` לכל המצב** — שמירה/טעינה כמעט "בחינם" בזכות תכנון נתונים נכון.
- **מערכת הסתברות אחוזית** — הצלחת חדר במבוך = פונקציה של המרחק בין התכונה לדרישה (טבלת אחוזים), לא זריקת קובייה עיוורת.
- **פירוק ה-ViewModel ל-Extensions** לפי תחום — קריאוּת ותחזוקה.
- **`Layout` protocol מותאם** (`FlowLayout`) — צ'יפים שנשברים לשורה חדשה רק בגלישה.
- **קבוצת קבצים מסונכרנת ב-Xcode** — קבצי `.swift` חדשים תחת `NightsOfZahara/` נקמפלים אוטומטית.

---

## 6. שאלות אפשריות מהמרצה + תשובות

**"למה בחרת MVVM ולא MVC / משהו אחר?"**
> SwiftUI דקלרטיבי ומבוסס observation — MVVM מתאים לו טבעית: ה-View מגיב לשינויים ב-`@Published`, וה-ViewModel מרכז את הלוגיקה. זה שומר על Views "טיפשים" וקלים לבדיקה.

**"איך נשמר המצב?"**
> כל המשחק ב-`GameState` שהוא `Codable`. `SaveManager` מקודד ל-JSON בתיקיית Documents, עם מראה ב-`UserDefaults` כ-fallback. שמירה מתבצעת אחרי כל פעולה.

**"מה קורה כשמוסיפים פיצ'ר חדש — שמירות ישנות נשברות?"**
> לא. כל התוכן המורחב יושב ב-`MetaState` אופציונלי אחד, וה-decoder משתמש ב-`decodeIfPresent ?? ברירת-מחדל`. שמירה ישנה בלי השדה פשוט מקבלת ערך ברירת מחדל.

**"איך מנוהל מצב בין המסכים?"**
> `GameViewModel` נוצר פעם אחת כ-`@StateObject` ב-`ContentView`, ומופץ לכל העץ כ-`@EnvironmentObject`. יש מקור-אמת יחיד.

**"איפה הלוגיקה של הקרב / המבוך?"**
> ב-`GameViewModel` — `enterDungeon`, `resolveCurrentRoom`, `resolveCombat`. ההצלחה מחושבת אחוזית מול דרישת החדר.

**"למה חילקת קובץ אחד לכמה קבצים?"**
> ה-ViewModel גדל, אז פירקתי אותו ל-Extensions לפי תחום (מלאי, משימות, דמויות) — Swift מאפשר לפרוס מחלקה על כמה קבצים, וזה שומר על כל קובץ ממוקד וקריא.

**"איך מתמודדים עם באגים / איך בדקת?"**
> הרצה חוזרת בסימולטור, טעינת שמירות בדיקה בשלבים שונים, ובדיקת build נקי אחרי כל שינוי.

---

## 7. מבנה הפרויקט (למקרה שישאלו)

```
NightsOfZahara/
├── NightsOfZaharaApp.swift   # נקודת כניסה
├── ContentView.swift         # Router ראשי + sheets גלובליים
├── Theme/                    # צבעים, פונטים, סגנונות כרטיסים
├── Models/                   # נתונים טהורים (Codable): Stats, GameState, Djinn, Dungeon...
├── Engine/                   # GameViewModel + Extensions, SaveManager, מנהלי שמע
├── Components/               # רכיבי UI לשימוש חוזר
└── Views/                    # מסך לכל קובץ (City, Actions, Character, Djinns, Inventory...)
```

---

## 8. טיפים להצגה

- **התחל מהתמונה הגדולה** (הדיאגרמה של MVVM) לפני שצוללים לקוד.
- **אל תקרא שורה-שורה** — הצבע על *הרעיון* בכל קובץ ותן דוגמה אחת.
- **הכן שמירה מתקדמת** מראש, כדי להדגים תוכן בלי לשחק 20 דקות.
- **תרגל את ה-Demo פעם אחת** לפני, כדי לוודא שהסימולטור עולה מהר.
- אם משהו נכשל בהדגמה — **הישאר רגוע**, הסבר מה *אמור* לקרות; המרצה מעריך הבנה יותר מהדגמה מושלמת.
- **סיים בנקודת החוזק שלך** (תאימות-לאחור לשמירות / ניהול המצב) — משאיר רושם.

בהצלחה! 🌙
